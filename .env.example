# Optional: OpenAI API Key (not required for demo with MockLLMProvider)
OPENAI_API_KEY=your_openai_api_key_here

# LLM Provider (mock | openai)
LLM_PROVIDER=mock

# OpenAI Model (if using OpenAI provider)
OPENAI_MODEL=gpt-4-turbo-preview

# Logging
LOG_LEVEL=info